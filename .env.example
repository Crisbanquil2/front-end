# Frontend environment variables (optional)
# Copy to .env and change only if your API runs on a different URL.

# Base URL for the Laravel API (no trailing slash)
VITE_API_BASE_URL=http://localhost:8000/api
